// 3 times table

// function to print 3 times table
function printTable(): void {
    let table = 3;

    // loop through numbers 1 to 10:
    for (let i = 1; i <= 10; i ++){
        console.log(`${table} x ${i} = ${table * i}`);
        
    }
}
printTable() // calling the function.